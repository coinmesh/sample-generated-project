{
  "name": "@coinmesh/bitcoin-wallet",
  "version": "0.0.1",
  "scripts": {
    "start": "npm install && concurrently \"nodemon ./bin/www\"",
    "test": "nodemon --exec 'jasmine'"
  },
  "dependencies": {
    "@coinmesh/bitcoind-adapter": "0.0.7",
    "concurrently": "^3.6.1"
  },
  "devDependencies": {
    "jasmine": "^3.2.0",
    "nodemon": "^1.18.3",
    "supertest": "^3.1.0"
  },
  "main": "index.js"
}
